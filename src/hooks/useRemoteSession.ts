/**
 * useRemoteSession — stub for COURTEOUSCODE (remote sessions not supported).
 * Her has the full implementation; this is a no-op to satisfy imports.
 */
export function useRemoteSession() {
  // No-op: COURTEOUSCODE does not support remote sessions (Telegram, Feishu, etc.)
}
