/**
 * Reserved compatibility surface. App self-update is intentionally disabled
 * until this fork owns a signed release channel and publication key.
 */
export function UpdateButton() {
  return null;
}
