# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# TypeScript incremental/compiler outputs (source configs stay as .ts)
*.tsbuildinfo
/vite.config.js
/vite.config.d.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust build artifacts
src-tauri/target/

# Installer files
*.dmg
*.msi
*.AppImage
*.deb
*.rpm

# Release artifacts (local build outputs — not tracked)
release-artifacts/

# Environment / secrets
.env
.env.*
!.env.example

# npm lock (project uses pnpm)
package-lock.json

# Obsidian
.obsidian/

# Updater JSON (release artifacts, not source)
latest.json
latest-*.json

# Internal / personal
bug-reports/
configs/

# AI workflow configs (local dev tooling, not project source)
CLAUDE.md
AGENTS.md
.claude/
.trellis/
.codex/
.agents/
.agent-log
.session-id

# Internal docs (not for public repo)
docs/cloud-thoughts*
docs/auto-compact*
docs/landing.html
docs/豆包*
screenshots/rewind.png
screenshots/slash-commands.png

# Test run outputs
.test/runs/
.test/automation-fixture.json

# Isolated local runtime for Agent development
.dev-runtime/
